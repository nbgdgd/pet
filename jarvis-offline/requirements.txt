# === Распознавание речи ===
onnx-asr[cpu,hub]>=0.12.0   # GigaAM v3 — основной офлайн-движок, ~8% WER на русском
silero-vad>=6.2.1           # детектор речи (endpointing)
faster-whisper>=1.2.1       # офлайн-движок, единственный выводит латиницу как латиницу
vosk>=0.3.45                # запасной офлайн-движок (старая маленькая модель)
deepgram-sdk>=7.6.0         # онлайн, опционально; нужен DEEPGRAM_API_KEY в окружении

# === Синтез речи ===
torch>=2.6                  # Silero TTS v5 (48 кГц) + silero-vad
piper-tts>=1.6.0            # запасной синтез (22 кГц)

# === Аудио ===
sounddevice>=0.5.4
numpy>=1.26.0

# === Логика ===
rapidfuzz>=3.14.3           # нечёткое сопоставление команд и названий программ
psutil>=6.0.0

# === Windows ===
pycaw>=20251023             # управление громкостью
comtypes>=1.4.15
pywin32>=311                # SAPI5 как последний запасной синтез
pillow>=10.0.0              # скриншоты
colorama>=0.4.6
